{
  "name": "@hummbl/router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "validate:bindings": "node scripts/validate-bindings.mjs",
    "test": "npm run validate:bindings && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^20.17.10"
  }
}
