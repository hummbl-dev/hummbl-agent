{
  "name": "hummbl-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "lint:schemas": "ajv compile -s schemas/*.schema.json -r schemas/enums/*.json -r schemas/*.schema.json --strict=false"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0",
    "fast-glob": "^3.3.2",
    "markdownlint-cli2": "^0.20.0",
    "yaml": "^2.5.0"
  }
}
