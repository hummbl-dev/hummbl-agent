{
  "version": "0.1.0",
  "allowedSecrets": [
    "OPENAI_API_KEY",
    "ANTHROPIC_API_KEY",
    "XAI_API_KEY"
  ],
  "requiredPrefix": "",
  "policy": {
    "noLogging": true,
    "noPersistence": true,
    "noPromptInjection": true
  },
  "notes": "Add or remove secrets explicitly; do not log or persist secrets."
}
